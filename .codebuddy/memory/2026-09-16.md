# 2026-09-16

- 排查用户遇到的 `std::bad_function_call`：最可能原因是 `luos/scene` 的 `data`（unordered_map<string, std::function>，中文别名 数据）用 `operator[]` 调用不存在的键，插入空 function 后调用抛异常。建议加 `Scene::call`（先 find 再调用）。次要隐患：`luos/run/english/router.inl` 中 `private_start` 在自身执行期间被重新赋值（std::function 自替换），建议改状态机。用户尚未确认是否添加 call 方法。
